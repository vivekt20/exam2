import 'dart:io';
void oddNumbers(int num1,int num2){
  for(num1=0;num1<=num2;num1++){
    if(num1%2!=0){

   }
  }
  void main(){
    stdout.write("enter the intrvals");
    int? num1=int.parse(stdin.readLineSync()!);
    int? num2=int.parse(stdin.readLineSync()!);
    oddNumbers(num1,num2);
  }
  
}
